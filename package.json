{
  "name": "fullstack-challenge",
  "private": true,
  "workspaces": ["client", "server", "shared"],
  "scripts": {
    "build": "npm run build -w shared && npm run build -w server && npm run build -w client",
    "build:shared": "npm run build -w shared",
    "dev:server": "npm run build -w shared && npm run dev -w server",
    "dev:client": "npm run build -w shared && npm run dev -w client",
    "test": "npm test -w shared && npm test -w server && npm test -w client",
    "test:shared": "npm test -w shared",
    "test:server": "npm test -w server",
    "test:client": "npm test -w client"
  }
}
