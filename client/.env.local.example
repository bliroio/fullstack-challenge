# Base URL of the API (without trailing resource path).
# Individual resource paths (e.g. /meetings) are appended in the client.
# Must be prefixed with NEXT_PUBLIC_ to be exposed to the browser.
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/api
